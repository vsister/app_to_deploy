import * as React from 'react';
import c from "./Popup.scss";

const Popup = (props) => {
    return (
        <div className={c.popup + " " + c.popup_theme}>
            something
        </div>
    )
}

export default Popup;